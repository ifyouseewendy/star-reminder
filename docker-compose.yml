version: '3'
services:
  redis_db:
    image: redis:4.0
    volumes:
      - data:/data
  script:
    build: .
    volumes:
      - .:/app
    env_file:
      - ./.production.env
    depends_on:
      - redis_db
volumes:
  data:
